{
  "name": "ftable-root",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/demo",
    "build": "npm run build --workspace=apps/demo",
    "typecheck": "npm run typecheck --workspace=packages/ftable"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
